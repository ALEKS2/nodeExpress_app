extends templates
block content
  h1=article.title
  small.text-info by #{author}
  p=article.body
  hr
  if user
   if user.id==article.author
    a.btn.btn-info(href="/articles/edit/"+article.id) Edit
    a.btn.btn-danger#delete_article(href="#",data-id=article.id) DELETE